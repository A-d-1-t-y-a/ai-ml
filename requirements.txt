# Time Series Forecasting Project Requirements - Python 3.13 Windows Compatible
# Using pre-compiled wheels only to avoid compilation issues

# Core data manipulation and analysis
pandas>=2.2.0
numpy>=2.0.0
scipy>=1.14.0

# Machine Learning and Statistics
scikit-learn>=1.5.0
xgboost>=2.1.0
statsmodels>=0.14.0
joblib>=1.4.0

# Financial data and technical analysis
yfinance>=0.2.18
pandas-ta>=0.3.14b0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.15.0

# Web dashboard
streamlit>=1.35.0
dash>=2.15.0

# AWS Services (optional - install separately if needed)
# boto3>=1.35.0
# sagemaker>=2.200.0

# Utilities
pytz>=2024.1
python-dateutil>=2.9.0
requests>=2.32.0
urllib3>=2.2.0

# Development and testing
pytest>=8.0.0

# Windows compatibility
setuptools>=75.0.0
wheel>=0.44.0
pip>=24.0.0

# Note: Some packages removed due to Python 3.13 compatibility:
# - TensorFlow (not yet available for Python 3.13)
# - HDBSCAN (compilation issues on Windows)
# - Some AWS libraries (can be installed separately if needed)
# 
# Alternative approaches:
# - Use DBSCAN from scikit-learn instead of HDBSCAN
# - Use scikit-learn MLPRegressor instead of LSTM for now
# - Install TensorFlow when Python 3.13 support is available
