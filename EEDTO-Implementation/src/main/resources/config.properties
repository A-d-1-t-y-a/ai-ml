# EEDTO Simulation Configuration

# Simulation parameters
simulation.duration=300
simulation.timeStep=0.1

# Device counts
devices.iot=10
devices.edge=3
devices.cloud=1

# Baseline configuration
baseline.energyWeight=0.33
baseline.latencyWeight=0.33
baseline.securityWeight=0.33
baseline.energyThreshold=0.2
baseline.latencyThreshold=5
baseline.securityLevel=3
baseline.blockchainDifficulty=2

# Energy-focused configuration
energy.energyWeight=0.6
energy.latencyWeight=0.2
energy.securityWeight=0.2
energy.energyThreshold=0.2
energy.latencyThreshold=5
energy.securityLevel=3
energy.blockchainDifficulty=2

# Latency-focused configuration
latency.energyWeight=0.2
latency.latencyWeight=0.6
latency.securityWeight=0.2
latency.energyThreshold=0.2
latency.latencyThreshold=5
latency.securityLevel=3
latency.blockchainDifficulty=2

# Security-focused configuration
security.energyWeight=0.2
security.latencyWeight=0.2
security.securityWeight=0.6
security.energyThreshold=0.2
security.latencyThreshold=5
security.securityLevel=3
security.blockchainDifficulty=2

# Task generation parameters
task.lightweightProbability=0.6
task.mediumProbability=0.3

# Lightweight task parameters
task.lightweight.size.min=10
task.lightweight.size.max=100
task.lightweight.mi.min=100
task.lightweight.mi.max=1000
task.lightweight.deadline.min=1
task.lightweight.deadline.max=5

# Medium task parameters
task.medium.size.min=100
task.medium.size.max=1000
task.medium.mi.min=1000
task.medium.mi.max=10000
task.medium.deadline.min=5
task.medium.deadline.max=20

# Intensive task parameters
task.intensive.size.min=1000
task.intensive.size.max=10000
task.intensive.mi.min=10000
task.intensive.mi.max=100000
task.intensive.deadline.min=20
task.intensive.deadline.max=60

# Device parameters
# IoT device parameters
iotDevice.mips.min=500
iotDevice.mips.max=1000
iotDevice.batteryCapacity.min=5000
iotDevice.batteryCapacity.max=10000
iotDevice.transmissionRange.min=100
iotDevice.transmissionRange.max=200
iotDevice.bandwidth.min=1
iotDevice.bandwidth.max=10
iotDevice.transmissionPower=0.1
iotDevice.executionPower=0.05
iotDevice.idlePower=0.01

# Edge server parameters
edgeServer.mips.min=5000
edgeServer.mips.max=10000
edgeServer.transmissionRange.min=500
edgeServer.transmissionRange.max=1000
edgeServer.bandwidth.min=50
edgeServer.bandwidth.max=100
edgeServer.costPerTask.min=0.05
edgeServer.costPerTask.max=0.10
edgeServer.transmissionPower=0.2

# Cloud server parameters
cloudServer.mips.min=20000
cloudServer.mips.max=50000
cloudServer.bandwidth.min=100
cloudServer.bandwidth.max=1000
cloudServer.costPerTask.min=0.20
cloudServer.costPerTask.max=0.50
cloudServer.transmissionPower=0.3

# Network parameters
network.lanBandwidth=100
network.wanBandwidth=10
network.lanLatency=0.005
network.wanLatency=0.05

# Security levels
security.iotDeviceLevel=2
security.edgeServerLevel=4
security.cloudServerLevel=5
security.defaultLevel=1

# Chart parameters
chart.width=800
chart.height=600
chart.localExecutionColor=0,153,51
chart.edgeOffloadColor=51,153,255
chart.cloudOffloadColor=153,51,255
chart.failedOffloadColor=255,51,51
chart.costColor=255,153,0
